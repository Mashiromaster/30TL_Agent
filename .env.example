# F_Agent 环境变量配置
# 复制此文件为 .env 并填入真实的 key（.env 已加入 .gitignore，不会提交）

# DeepSeek API Key（用于 AI 情报 / RAG 检索 / 交易记忆反思）
DEEPSEEK_API_KEY=***

# DeepSeek 模型名称（可选，默认 deepseek-chat）
# DEEPSEEK_MODEL=deepseek-chat
