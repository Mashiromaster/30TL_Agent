---
name: dashboard
description: Launch the Streamlit dashboard (7 tabs)
---

Start the Streamlit dashboard on port 8501.

```bash
cd "D:\桌面\F_Agent\src" && python -m streamlit run dashboard.py --server.port 8501
```
