---
name: signal
description: Run inference mode — generate today's trading signal
---

Run inference to generate real-time trading signals.

```bash
cd "D:\桌面\F_Agent\src" && python main.py --mode inference
```

Then display the current signal: direction, confidence, and position recommendation.
